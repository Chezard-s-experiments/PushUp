// Entry point par défaut (environnement DEV).
// Pour changer d'environnement : flutter run --dart-define=ENV=staging
import 'package:pushup_hub/core/config/bootstrap.dart';

void main() => bootstrap();
