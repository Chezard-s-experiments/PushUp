import 'package:flutter/material.dart';

/// Non-web : aucun widget (le parent utilise [AppButton]).
Widget buildWebGoogleSignInButton(
  BuildContext context, {
  required bool forSignUp,
}) {
  return const SizedBox.shrink();
}
